<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - C1Soft B2B</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <style>
        .navbar-brand { font-weight: 700; letter-spacing: -0.5px; }
        .nav-link.active { font-weight: 600; color: #0d6efd !important; }
        .cart-badge { font-size: 0.75rem; vertical-align: top; }
        .badge-stock-in { background-color: #198754; color: #fff; }
        .badge-stock-critical { background-color: #ffc107; color: #000; }
        .badge-stock-out { background-color: #dc3545; color: #fff; }
    </style>
</head>
<body class="d-flex flex-column min-vh-100 bg-light">
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
            <div class="container">
                <a class="navbar-brand text-white" asp-controller="Home" asp-action="Index">
                    <i class="bi bi-boxes text-primary me-1"></i>C1Soft <span class="badge bg-primary">B2B</span>
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain"
                        aria-controls="navbarMain" aria-expanded="false" aria-label="Menüyü Aç">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarMain">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link text-white-50" asp-controller="Home" asp-action="Index">
                                <i class="bi bi-house me-1"></i>Ana Sayfa
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link text-white-50" asp-controller="Product" asp-action="Index">
                                <i class="bi bi-grid-3x3-gap me-1"></i>Ürün Kataloğu
                            </a>
                        </li>
                        @if (User.Identity?.IsAuthenticated == true && !User.IsInRole("Admin"))
                        {
                            <li class="nav-item">
                                <a class="nav-link text-white-50" asp-controller="Order" asp-action="MyOrders">
                                    <i class="bi bi-receipt me-1"></i>Siparişlerim
                                </a>
                            </li>
                        }
                        @if (User.IsInRole("Admin"))
                        {
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle text-warning" href="#" role="button" data-bs-toggle="dropdown">
                                    <i class="bi bi-shield-lock me-1"></i>Yönetim Paneli
                                </a>
                                <ul class="dropdown-menu dropdown-menu-dark">
                                    <li><a class="dropdown-item" asp-controller="Admin" asp-action="Orders"><i class="bi bi-receipt me-2"></i>Sipariş Yönetimi</a></li>
                                    <li><a class="dropdown-item" asp-controller="Admin" asp-action="Products"><i class="bi bi-box-seam me-2"></i>Ürün Yönetimi</a></li>
                                    <li><a class="dropdown-item" asp-controller="Admin" asp-action="Users"><i class="bi bi-people me-2"></i>Bayi & Kullanıcılar</a></li>
                                </ul>
                            </li>
                        }
                    </ul>

                    <ul class="navbar-nav ms-auto align-items-center">
                        @if (User.Identity?.IsAuthenticated == true)
                        {
                            @if (!User.IsInRole("Admin"))
                            {
                                <li class="nav-item me-3">
                                    <a class="btn btn-outline-light position-relative btn-sm px-3" asp-controller="Cart" asp-action="Index">
                                        <i class="bi bi-cart3 me-1"></i>Sepetim
                                        <span id="navCartCount" class="badge rounded-pill bg-danger cart-badge ms-1">0</span>
                                    </a>
                                </li>
                            }

                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle text-white d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown">
                                    <i class="bi bi-person-circle me-2 fs-5"></i>
                                    <span>
                                        @User.Identity.Name
                                        <span class="badge @(User.IsInRole("Admin") ? "bg-danger" : "bg-success") ms-1">
                                            @(User.IsInRole("Admin") ? "Admin" : "Bayi")
                                        </span>
                                    </span>
                                </a>
                                <ul class="dropdown-menu dropdown-menu-end shadow">
                                    <li>
                                        <form asp-controller="Auth" asp-action="Logout" method="post" class="d-inline">
                                            @Html.AntiForgeryToken()
                                            <button type="submit" class="dropdown-item text-danger">
                                                <i class="bi bi-box-arrow-right me-2"></i>Çıkış Yap
                                            </button>
                                        </form>
                                    </li>
                                </ul>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item me-2">
                                <a class="btn btn-outline-light btn-sm" asp-controller="Auth" asp-action="Login">
                                    <i class="bi bi-box-arrow-in-right me-1"></i>Giriş Yap
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="btn btn-primary btn-sm" asp-controller="Auth" asp-action="Register">
                                    <i class="bi bi-person-plus me-1"></i>Bayi Ol
                                </a>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <div class="container my-4 flex-grow-1">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="bg-white border-top py-3 mt-auto text-muted">
        <div class="container text-center small">
            &copy; 2026 <strong>C1Soft Mini B2B E-Ticaret Platformu</strong> - Tüm Hakları Saklıdır.
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @if (User.Identity?.IsAuthenticated == true && !User.IsInRole("Admin"))
    {
        <script>
            // Sepet rozeti güncelleme fonksiyonu
            function refreshCartBadge() {
                fetch('/Cart/GetCartCount')
                    .then(res => res.json())
                    .then(data => {
                        const el = document.getElementById('navCartCount');
                        if (el) el.textContent = data.count || 0;
                    })
                    .catch(() => {});
            }
            document.addEventListener('DOMContentLoaded', refreshCartBadge);
        </script>
    }
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
